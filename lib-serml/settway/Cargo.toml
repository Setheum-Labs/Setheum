[package]
name = "setheum-settway"
version = "0.5.0"
authors = ["Setheum Labs"]
edition = "2018"

[dependencies]
# external dependencies
serde = { version = "1.0.101", optional = true }
codec = { package = "parity-scale-codec", version = "2.0.0", default-features = false }

# Substrate dependencies
sp-runtime = { default-features = false, version = '3.0.0', git = 'https://github.com/paritytech/substrate.git', tag = 'monthly-2021-05' }
frame-support = { default-features = false, version = '3.0.0', git = 'https://github.com/paritytech/substrate.git', tag = 'monthly-2021-05' }
frame-system = { default-features = false, version = '3.0.0', git = 'https://github.com/paritytech/substrate.git', tag = 'monthly-2021-05' }
sp-std = { default-features = false, version = '3.0.0', git = 'https://github.com/paritytech/substrate.git', tag = 'monthly-2021-05' }

# orml dependencies
orml-tokens = { path = "../../lib-orml/tokens", default-features = false }

# local dependencies
settmint-engine = { package = "setheum-settmint-engine", path = "../settmint-engine", default-features = false }
setters = { package = "setheum-setters", path = "../setters", default-features = false }
support = { package = "setheum-support", path = "../support", default-features = false }
primitives = { package = "setheum-primitives", path = "../../primitives", default-features = false }

[dev-dependencies]
sp-io = "3.0.0"
sp-core = "3.0.0"
pallet-balances = "3.0.0"
orml-currencies = { path = "../../lib-orml/currencies" }
orml-traits = { path = "../../lib-orml/traits" }
serp-treasury = { package = "serp-treasury", path = "../serp-treasury" }

[features]
default = ["std"]
std = [
	"serde",
	"codec/std",
	"sp-runtime/std",
	"frame-support/std",
	"frame-system/std",
	"sp-std/std",
	"orml-tokens/std",
	"setters/std",
	"settmint-engine/std",
	"support/std",
	"primitives/std",
]
try-runtime = ["frame-support/try-runtime"]
